# methods for set 
# set.add(value)

set = {1,5,32,5,5,5}
set.add(89) # adds the element to the set

length_of_set = len(set) # length of the set

set.remove(1) # Updates the set & removes the element from the set 

set.pop() #Removes random element from the set and returns the element removed

set.clear() #Clears the set

# Methods for two sets
set1 = {1,2,3}
set2 = {2,3,4}

union = set1.union(set2) # Displays both elements of 2 sets
# Displays {1,2,3,4}
intersection = set1.intersection(set2) # Displays matching elements of 2 sets
# Displays {2,3}
diff = set1.difference(set2) # Subtracts same element from 2nd set then displays element only 1st set only
# Displays {1}





