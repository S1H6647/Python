# Ways to remove i’th character from string in Python

enteredString = input("Enter a string: ") # Hello 
userRemove = input("Which string do yo want to remove: ") # e

removedWord = enteredString.replace(userRemove, "")

print(removedWord)